/* Здесь будут заметки, которые было бы лучше сохранить */
/* Но в мейн коде они будут мешатся */



/*
<center>
  <h1>{count}</h1>
  <button onClick={() => plus()}>
    +
</button>
  <button onClick={() => minus()}>
     -
  </button>
</center>
*/

/*
const [count, setCount] = React.useState(0)

const plus = () => {
  setCount(count + 1)
}

const minus = () => {
  setCount(count - 1)
}

React.useEffect(() => {
    console.log(isAdded)
  }, [isAdded, действие два, три, четыре])

  // useEffect Нужен для того, чтобы отслеживать (Вожможно при бэкенде понадобится)

  {cartOpened ? <Drawer onClose={() => setCartOpened(false)} /> : null}
  {cartOpened && <Drawer onClose={() => setCartOpened(false)} />}
  
  axios
  .get("https://662398043e17a3ac846fa3bf.mockapi.io/cart")
  .then((res) => {
    setCartItems(res.data);
  });
  axios
  .get("https://662810af54afcabd0734c77d.mockapi.io/favorites")
  .then((res) => {
    setFavorites(res.data);
    console.log(res.data);
  });
}

      <ContentLoader
        speed={2}
        width={155}
        height={265}
        viewBox="0 0 155 265"
        backgroundColor="#f3f3f3"
        foregroundColor="#ecebeb"
      >
        <rect x="1" y="0" rx="10" ry="10" width="155" height="155" />
        <rect x="0" y="167" rx="5" ry="5" width="155" height="15" />
        <rect x="0" y="187" rx="5" ry="5" width="100" height="15" />
        <rect x="1" y="234" rx="5" ry="5" width="80" height="25" />
        <rect x="124" y="230" rx="10" ry="10" width="32" height="32" />
      </ContentLoader>

*/